import { LocalStorageWrapper } from './local-storage-wrapper';

describe('LocalStorageWrapper', () => {
  it('should create an instance', () => {
    expect(new LocalStorageWrapper()).toBeTruthy();
  });
});
